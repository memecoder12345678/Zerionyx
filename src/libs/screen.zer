namespace screen
    defun capture(p)
        return screen_capture_fp(p)
    done

    defun capture_area
        return screen_capture_area_fp
    done

    defun get_color(x, y)
        return screen_get_color(x, y)
    done
done
