namespace screen
    defun capture(p)
        screen_capture_fp(p)
        return none
    done

    defun capture_area
        screen_capture_area_fp
    done

    defun get_color(x, y)
        screen_get_color(x, y)
    done
done
