# libs.time

namespace time
    defun sleep(s)
        sleep_fp(s)
        return none
    done

    defun time()
        return time_fp()
    done

    defun ctime(t)
        return ctime_fp(t)
    done
done